server {
    listen 80;

    # Allow uploads up to 20MB (ID card + signature images)
    client_max_body_size 20m;

    # Static files
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # API Proxy
    location /api {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120s;
    }

    # Handle Uploads (Directly from shared volume if needed, or proxy back to API)
    # The API service manages the physical /app/uploads, but we proxy to it for simplicity
    location /uploads {
        proxy_pass http://api:3000/uploads;
        proxy_set_header Host $host;
    }
}
